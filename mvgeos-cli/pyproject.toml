[project]
name = "mvgeos-cli"
version = "0.1.0"
description = "CLI entry point for MvgeOS"
requires-python = ">=3.14"
license = {text = "MIT"}
dependencies = [
    "mvgeos-agent",
    "mvgeos-provider",
    "mvgeos-tome",
    "mvgeos-spells",
    "mvgeos-runes",
    "rich>=13.0",
]

[project.scripts]
mvgeos = "mvgeos.main:main"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]

[tool.uv.sources]
mvgeos-agent = { workspace = true }
mvgeos-provider = { workspace = true }
mvgeos-tome = { workspace = true }
mvgeos-spells = { workspace = true }
mvgeos-runes = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mvgeos"]

[tool.ruff]
extend = "../../../pyproject.toml"