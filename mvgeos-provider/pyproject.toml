[project]
name = "mvgeos-provider"
version = "0.1.0"
description = "Provider abstraction protocol and OpenRouter implementation"
requires-python = ">=3.14"
license = {text = "MIT"}
dependencies = [
    "httpx>=0.27",
    "filelock>=3.14",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
extend = "../../../pyproject.toml"