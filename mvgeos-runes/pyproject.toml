[project]
name = "mvgeos-runes"
version = "1.14.1"
description = "Extension system: manifest, loader, and sigil hook protocol"
requires-python = ">=3.13"
license = "MIT"
dependencies = [
    "pyyaml>=6.0",
    "watchdog>=6.0.0",
]

[build-system]
requires = ["uv_build>=0.11.2,<0.13"]
build-backend = "uv_build"

[tool.ruff]
extend = "../pyproject.toml"
